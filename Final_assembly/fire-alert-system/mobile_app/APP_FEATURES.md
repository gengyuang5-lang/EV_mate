# 移动应用功能说明

## ✅ 已实现功能

### 1. 报警功能 🔔
- ✅ **实时预警接收**：通过WebSocket实时接收火灾预警信息
- ✅ **预警列表显示**：AlertScreen显示所有活跃预警
- ✅ **预警弹窗**：收到新预警时自动弹出模态窗口
- ✅ **预警标记**：地图上用不同颜色标记不同级别的预警
- ✅ **预警解决**：可以标记预警为已解决

### 2. 逃生路线显示功能 🗺️
- ✅ **路径规划**：自动计算从当前位置到最近出口的逃生路线
- ✅ **地图显示**：在地图上显示逃生路线（虚线标记）
- ✅ **实时定位**：获取用户当前位置
- ✅ **火点避让**：路径规划会自动避开火点区域
- ✅ **路线信息**：显示路线距离和预计时间
- ✅ **路线刷新**：可以重新计算路线

### 3. 其他功能
- ✅ **多语言支持**：支持中文和英文切换
- ✅ **一键求助**：发送求助请求到服务器
- ✅ **地图浏览**：实时火点地图显示
- ✅ **导航系统**：底部标签导航，支持多个页面

## 📱 应用页面

1. **首页 (HomeScreen)**
   - 显示实时火点地图
   - 显示活跃预警数量
   - 逃生路线按钮

2. **预警页面 (AlertScreen)**
   - 预警列表
   - 预警详情
   - 预警解决功能

3. **逃生路线页面 (EscapeRouteScreen)**
   - 当前位置标记
   - 出口位置标记
   - 逃生路线显示
   - 路线信息面板

4. **求助页面 (HelpScreen)**
   - 一键发送求助请求
   - 位置信息显示

5. **设置页面 (SettingsScreen)**
   - 语言切换
   - 连接状态显示

## 🚀 运行应用

### 前提条件
1. Node.js >= 18
2. Android Studio (Android开发) 或 Xcode (iOS开发，仅macOS)
3. React Native CLI

### 初始化原生代码（首次运行）

由于项目还没有Android/iOS原生代码目录，需要初始化：

```bash
cd fire-alert-system/mobile_app

# 如果使用React Native CLI，需要初始化原生项目
# 注意：这会创建android/ios目录，可能会覆盖现有代码
# 建议先备份或使用新项目

# 方式1：使用npx react-native init（不推荐，会创建新项目）
# npx react-native init FireAlertApp

# 方式2：手动初始化（推荐）
# 从react-native模板复制android/ios目录
# 或者使用react-native upgrade命令

# 方式3：使用Expo（如果项目支持）
# npx expo start
```

### 运行步骤

1. **启动后端服务**（必需）
```bash
cd fire-alert-system/backend
npm start
```

2. **启动传感器模拟器**（可选，用于测试）
```bash
cd fire-alert-system/sensors
npm start
```

3. **启动Metro Bundler**
```bash
cd fire-alert-system/mobile_app
npm start
```

4. **运行Android应用**
```bash
# 在新终端
cd fire-alert-system/mobile_app
npm run android
```

5. **运行iOS应用**（仅macOS）
```bash
cd fire-alert-system/mobile_app
npm run ios
```

## 📝 功能演示流程

1. **启动应用**
   - 应用会自动连接后端服务
   - 首页显示火点地图

2. **查看预警**
   - 点击底部"预警"标签
   - 查看预警列表
   - 有新预警时会自动弹窗

3. **使用逃生路线**
   - 点击首页的"逃生路线"按钮
   - 允许位置权限
   - 查看计算的逃生路线
   - 路线会避开火点区域

4. **发送求助**
   - 点击底部"求助"标签
   - 点击"发送求助请求"按钮

## 🔧 技术栈

- React Native 0.72.6
- React Navigation 6.x
- React Native Maps
- WebSocket (实时通信)
- React i18next (多语言)

## 📂 关键文件

- `src/screens/HomeScreen.js` - 首页
- `src/screens/AlertScreen.js` - 预警页面
- `src/screens/EscapeRouteScreen.js` - 逃生路线页面
- `src/services/pathPlanning.js` - 路径规划服务
- `src/services/api.js` - API服务
- `src/services/websocket.js` - WebSocket服务
- `src/components/AlertPopup.js` - 预警弹窗组件

## ⚠️ 注意事项

1. **位置权限**：逃生路线功能需要位置权限
2. **后端服务**：应用需要后端服务运行才能正常工作
3. **地图API**：Android需要Google Maps API Key（在android/app/src/main/AndroidManifest.xml中配置）
4. **网络连接**：需要网络连接以连接后端和WebSocket

## 🎯 后续开发建议

- [ ] 添加语音播报功能（TTS）
- [ ] 添加推送通知
- [ ] 优化路径规划算法
- [ ] 添加离线模式支持
- [ ] 添加导航指引功能
- [ ] 添加建筑物平面图显示

